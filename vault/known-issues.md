# Known Issues

## 2026-03-24
- No platform-specific issues. All dependencies are pure Python with pre-built wheels.
- `asusguard_daemon` and legacy `asusguard` share port 8088 — only one can run at a time.
